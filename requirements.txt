--extra-index-url https://aiinfra.pkgs.visualstudio.com/PublicPackages/_packaging/onnxruntime-cuda-12/pypi/simple/

# Core ML / image
numpy>=1.23.5,<2
opencv-python>=4.7.0
pillow>=9.5.0
onnx>=1.14.0
onnxruntime-gpu==1.18.1
insightface==0.7.3
protobuf>=4.23.2
tqdm
psutil>=5.9.8

# cuDNN 9 — required by onnxruntime-gpu 1.18.1 CUDA 12 build
nvidia-cudnn-cu12

# Server
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
msgpack>=1.0.7
websockets>=12.0
python-dotenv>=1.0.0

# Client (face detection — CPU-only, no GPU needed on client machine)
mediapipe>=0.10.9

# Voice conversion (voice_convert.py) — install separately if needed
# uv pip install sounddevice soundfile scipy rvc-python
